# Rottra Embedding Fine-Tune Dependencies
# Install: pip install -r requirements.txt

# Core training
torch>=2.1.0
sentence-transformers>=3.3.0
datasets>=2.19.0
transformers>=4.44.0
accelerate>=0.33.0

# Fine-tune losses
# MultipleNegativesRankingLoss, MatryoshkaLoss built into sentence-transformers

# Export
optimum[exporters]>=1.22.0
onnxruntime>=1.18.0

# Utils
numpy>=1.26.0
tqdm>=4.66.0
wandb>=0.17.0          # Optional: experiment tracking
