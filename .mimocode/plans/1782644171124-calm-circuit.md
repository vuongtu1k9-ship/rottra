# Plan: Kích hoạt ACE-Step Music Engine — Nhẹ nhất

## Context

User hỏi "cách nào tốt nhất nhẹ nhất" để tích hợp ACE-Step music cho 12 agent bán hàng.

**Hiện trạng:**
- `music-engine.ts` — ACE-Step client đã viết xong, **chưa mount** (dead code)
- `agent-music.ts` — Hono routes đã viết xong, **chưa mount** (dead code)
- `local-media-engine.ts` — Local WAV synthesis **đang chạy** (low quality, sine waves)
- `media-engine.ts` — **Duplicate** của music-engine.ts (dead code)

**Giải pháp nhẹ nhất:** Kích hoạt code đã có + cleanup duplicate.

## Changes

### 1. Mount `musicApp` routes vào catch-all route

**File:** `src/routes/api/[...paths].ts`
- Import `musicApp` from `~/server/api/agent-music`
- Mount tại `/music` (hoặc `/api/music`)
- Vị trí: gần line 8921 nơi `mediaApp` đang được mount

### 2. Thêm env vars vào `.env`

**File:** `.env`
```
ACESTEP_API_URL=http://localhost:3001
ACESTEP_GRADIO_URL=http://localhost:8001
```

### 3. Cleanup duplicate code

**File:** `src/server/api/media-engine.ts` — Xóa file này (duplicate của music-engine.ts)

### 4. Route endpoints sẽ có

Sau khi mount, các endpoint hoạt động:

| Method | Endpoint | Mô tả |
|--------|----------|-------|
| POST | `/api/music/generate` | Tạo nhạc cho agent (ACE-Step) |
| GET | `/api/music/profiles` | Xem 12 agent music profiles |
| POST | `/api/music/detect-mood` | Phân tích mood từ text |

### 5. Fallback behavior

`getAgentBackgroundMusic()` đã có fallback tự động:
- Thử ACE-Step UI server (port 3001) → REST API
- Nếu fail → Thử Gradio direct (port 8001)
- Nếu cả hai fail → trả `{ audioUrl: null }` (client dùng local WAV hoặc skip)

## Files to modify

| File | Action |
|------|--------|
| `src/routes/api/[...paths].ts` | Import + mount `musicApp` |
| `.env` | Thêm 2 env vars |
| `src/server/api/media-engine.ts` | Xóa (duplicate) |

## Verification

1. `bun x tsc --noEmit` — Type check pass
2. `./init.sh` — Build pass
3. Test endpoint: `curl -X POST http://localhost:5173/api/music/profiles`
4. Test generate (nếu ACE-Step đang chạy): `curl -X POST http://localhost:5173/api/music/generate -H "Content-Type: application/json" -d '{"agentId":"toLuong","mood":"neutral"}'`
